<DisplayArea as |sim|>
  <Panel @title="Electrical">
    <ValueBar @title="Bus A Load"
              @min={{0}} @max={{1}}
              @value={{sim.simState.b1.currentSensor.i}}
              @unit="A"/>
  </Panel>
  <Panel @title="Breakers">
    <Breaker @title="B1" @isOpen={{sim.simState.b1.isOpen}} @open="b1Open" @close="b1Close"/>
  </Panel>
</DisplayArea>
